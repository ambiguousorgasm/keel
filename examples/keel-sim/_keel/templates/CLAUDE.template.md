# Claude Code — read AGENTS.md

This file exists because Claude Code looks for `CLAUDE.md` in the project root.
The canonical operational contract for every agent (Claude Code, Codex, Gemini,
others) lives in [`AGENTS.md`](./AGENTS.md). Read that file before anything
else, then return here only for Claude-Code-specific extensions (if any).

**Do not maintain two parallel rule sets.** Project-wide policies belong in
`AGENTS.md`. Tool-specific quirks may be added below this line.

---

## Claude-Code-specific notes

<add any Claude-Code-specific instructions here — slash command paths, MCP
server expectations, etc. Otherwise leave blank.>
